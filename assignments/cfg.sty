\usepackage{tikz}
\usetikzlibrary{positioning, arrows, arrows.meta, calc, tikzmark, graphs, graphdrawing, babel, decorations.shapes,
intersections, quotes, fit, backgrounds, decorations.pathmorphing}
\usegdlibrary{layered, force, trees}
% amsmath,amsthm,
\usepackage{amssymb} %mathbb
\usepackage{mathtools} % rcases
\usepackage{amsthm} % theoremstyle
\usepackage{bm}
% \usepackage{ragged2e} 
\usepackage{tabularx}
\usepackage{nicematrix} %pyramid like tables - CYK
\usepackage{multicol}
\usepackage[shortlabels]{enumitem}
\usepackage{utfsym} %for cross mark
\usepackage{forest} %derivation trees
% \usepackage{soul}
\usepackage{float} %figure
\usepackage[autostyle]{csquotes} %smart quote handling
\usepackage[nopatch=item]{microtype}
\usepackage{fancyvrb}
\numberwithin{equation}{section}

\usepackage{hyperref}
\hypersetup{
    colorlinks=true,
    linktoc=all,                 % links in TOC for sections and subsections
    linkcolor=purple,
    pdfborder=0 0 0              % no red boxes around links
}

\tikzset{
    % ->, % makes the edges directed
    >=stealth', % makes the arrow heads bold
    node distance=3cm, % specifies the minimum distance between two nodes
    % dot/.style = {circle, fill, inner sep=2pt, node contents={}, label=#1},
    % every edge/.style = {draw, postaction=decorate},
    % every loop/.style={in=120,out=60,looseness=0},
}

\newcommand{\G}{\mathcal{G}}
\newcommand{\R}{\mathcal{R}}
\renewcommand{\S}{\mathcal{S}} % replaces paragraph symbol
\newcommand{\N}{\mathbb{N}}
\newcommand{\Z}{\mathbb{Z}}

\newcommand{\ii}[1]{\textit{#1}} % italic 
\newcommand{\bb}[1]{\textbf{#1}} % bold 
\newcommand{\m}[1]{$\mathbf{#1}$}
\newcommand{\bc}[1]{\left\lbrace #1 \right\rbrace}  % \{\} 
\newcommand{\eps}{\varepsilon}
\renewcommand{\qed}{\blacksquare}
\renewcommand{\eqref}[1]{\textup{\hyperref[#1]{(\ref*{#1})}}}

\theoremstyle{plain}
\newtheorem{exercise}{Příklad}[section]
\newtheorem*{lemma}{\small Lemma}
\theoremstyle{definition}
\newtheorem{solution}{Řešení}[section]
\newtheorem*{prof}{\small Důkaz}

\DeclareMathOperator*{\diam}{diam}
\DeclareMathOperator*{\ex}{ex}
\DeclareMathOperator*{\rad}{rad}
\DeclareMathOperator*{\fcap}{cap}
\DeclareMathOperator*{\vel}{vel}
\DeclarePairedDelimiter\ceil{\lceil}{\rceil}
\DeclarePairedDelimiter\floor{\lfloor}{\rfloor}

\setlength{\parindent}{0mm} 
\setlength{\parskip}{1.4ex}

\newcommand\setItemnumber[1]{\setcounter{enumi}{\numexpr#1-1\relax}}

\newcommand\titlebreak{~\\\vspace{-2em}}

\newcommand{\xmark}{\usym{2613}}
\newcommand{\dukaz}{\noindent\textsc{Důkaz}. }
\newcommand{\dusledek}{\noindent\textsc{Důsledek}. }